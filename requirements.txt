# Pinned for reproducibility across your NUC 16 and Gary's NUC 15.
# OpenVINO 2026.x is required for the Arrow Lake / Arc 140T iGPU and ships
# Python 3.14 wheels (matching the Ubuntu 26.04 base image).
# NOTE: PyTorch is installed separately in the Dockerfile from the CPU-only
# index so we don't drag in ~5GB of unused CUDA packages.
openvino==2026.0.0
openvino-genai==2026.0.0.0
ultralytics==8.4.56
opencv-python-headless==4.13.0.92
pillow
numpy
# System/hardware metrics (CPU, memory, temps) for telemetry.
psutil==7.1.0
# Avnet /IOTCONNECT lite SDK (X.509 auth, AWS platform).
iotconnect-sdk-lite==1.3.0
